//3. Написати функцію, яка прийматиме масив словників. В масиві має бути мінімум 10 елементів 
//(просто до того що перевірки на nil, порожність масиву та подібні - зайві). 
//Словник у нас буде “журналом оцінок”. Тобто, ключ - ім”я, значення - оцінка. Вирахувати середню оцінку цього журналу.
import Foundation
let schoolgirl: Set<String> = ["Lizetta", "Babbetta", "Jorjetta", "Closetta" ,"Fidetta", "Kabeta"]
var someDictArray : [[String: Int]]=[]
var someDict = [String: Int]()
for journalIndex in 0...10 {
    for listIndex in 0...5 {
        someDict[schoolgirl.randomElement()!] = Int.random(in: 0...12)
    }
  someDictArray.append(someDict)
    
}

func midleRating(_ inputJournal:[[String: Int]] ) -> Double {
    var sum: Double = 0
    var count: Int = 0
    var list = [String : Int]()
    var name : String
             for list in inputJournal {
                     for nameRated in list {
                        sum = sum + Double( nameRated.value)
                      count = count + 1
                    }  
                }
    
    return sum / Double(count)
}



print(someDictArray)
print(midleRating(someDictArray))
