import Foundation
//Створити функцію, яка на вході приймає рядок цілих чисел (не масив!) типу Int і повертає 
//тюпл (min: Int, max: Int, mediana: Float). 
//Приклад виклику: calculate(1,2,3) має повернути (min: 1, max:3, mediana: 2.0), 
//якщо викликати calculate(1,1,1,2,2,2,3,3,3) то має повернути те саме


func  calculate(_ numbers: Int...) -> (min: Int, max: Int, mediana: Float)? {   
 let n = numbers.sorted()
 guard n.count>0 else {return nil}
 guard n.count>1 else {return (n[0],n[0],Float(n[0]))}
 return (n[0], n[n.count-1], Float(n[n.count/2+n.count%2-1]+n[n.count/2])/2) 
} 

print(calculate(1))
print(calculate(1,3))
print(calculate(1,2,3))
print(calculate())
